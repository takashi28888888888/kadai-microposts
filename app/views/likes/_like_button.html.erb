<% if current_user.liking?(micropost) %>
    <%= form_with(model: current_user.likes.find_by(micropost_id: other_micropost.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'like', class: 'btn btn-danger btn-block' %>
  <% end %>
  <% else %>
    <%= form_with(model: current_user.likes.build, local: true) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'unlike', class: 'btn btn-primary btn-block' %>
  <% end %>
<% end %>
